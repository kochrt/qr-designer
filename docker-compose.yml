version: "2"
services:
  qr-generator:
    image: "node:16"
    user: "node"
    build:
      context: .
      dockerfile_inline: |
        FROM node:16
        RUN mkdir -p /home/node/app
        COPY . /home/node/app
        WORKDIR /home/node/app
        RUN npm install; 
        RUN npm run generate;

    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    expose:
      - "3000"
    ports:
      - "8081:3000"
    command: "npm start"
